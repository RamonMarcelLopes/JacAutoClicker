namespace JacaAutoClicker.Domain.ValueObjects;

/// <summary>Windows virtual-key codes the Domain needs by name, without depending on a UI framework's key enum.</summary>
internal static class VirtualKeys
{
    public const int F6 = 0x75;
}
